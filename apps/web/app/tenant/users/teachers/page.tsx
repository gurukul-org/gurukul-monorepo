import type { Metadata } from 'next';

import TeachersContainer from '@/containers/Tenant/Teachers';

export const metadata: Metadata = {
  title: 'Teachers | Gurukul',
  description: 'Browse teacher profiles on the platform.',
};

export default TeachersContainer;
