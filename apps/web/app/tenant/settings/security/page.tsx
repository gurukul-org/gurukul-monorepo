import type { Metadata } from 'next';

import SecurityContainer from '@/containers/Tenant/Settings/Security';

export const metadata: Metadata = {
  title: 'Security Settings | Gurukul',
  description: 'Manage your security credentials and password.',
};

export default SecurityContainer;
