import type { Metadata } from 'next';

import TeamContainer from '@/containers/Tenant/Settings/Team';

export const metadata: Metadata = {
  title: 'Team Settings | Gurukul',
  description: 'Manage your tenant members and roles.',
};

export default TeamContainer;
