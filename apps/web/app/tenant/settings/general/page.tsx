import type { Metadata } from 'next';

import GeneralContainer from '@/containers/Tenant/Settings/General';

export const metadata: Metadata = {
  title: 'General Settings | Gurukul',
  description: 'Manage general workspace preferences.',
};

export default GeneralContainer;
