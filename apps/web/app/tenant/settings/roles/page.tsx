import type { Metadata } from 'next';

import RolesContainer from '@/containers/Tenant/Settings/Roles';

export const metadata: Metadata = {
  title: 'Roles & Permissions Settings | Gurukul',
  description: 'Manage roles and custom capabilities for your workspace.',
};

export default RolesContainer;
