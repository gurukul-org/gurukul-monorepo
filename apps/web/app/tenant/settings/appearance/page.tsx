import type { Metadata } from 'next';

import AppearanceContainer from '@/containers/Tenant/Settings/Appearance';

export const metadata: Metadata = {
  title: 'Appearance | Gurukul',
  description: 'Choose your workspace theme, colors, radius, and font.',
};

export default AppearanceContainer;
