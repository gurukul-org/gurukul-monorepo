import type { Metadata } from 'next';

import TenantLogin from '@/containers/Tenant/Login';

export const metadata: Metadata = {
  title: 'Sign in | Gurukul',
  description: 'Sign in to your workspace.',
};

export default TenantLogin;
