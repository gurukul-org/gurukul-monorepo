import type { Metadata } from 'next';

import TenantClassesContainer from '@/containers/Tenant/Classes';

export const metadata: Metadata = {
  title: 'Class Management | Gurukul',
  description: 'Schedule and manage concrete classes for academic courses.',
};

export default TenantClassesContainer;
