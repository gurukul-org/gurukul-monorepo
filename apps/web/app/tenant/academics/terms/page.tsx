import type { Metadata } from 'next';

import TenantAcademicTermsContainer from '@/containers/Tenant/AcademicTerms';

export const metadata: Metadata = {
  title: 'Academic Term Management | Gurukul',
  description: 'Manage academic terms for your Gurukul workspace.',
};

export default TenantAcademicTermsContainer;
