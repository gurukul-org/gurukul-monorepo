import type { Metadata } from 'next';

import TenantCoursesContainer from '@/containers/Tenant/Courses';

export const metadata: Metadata = {
  title: 'Course Management | Gurukul',
  description: 'Manage academic courses, programs, and credit allocations.',
};

export default TenantCoursesContainer;
