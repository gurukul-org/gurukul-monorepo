import type { Metadata } from 'next';

import RolesContainer from '@/containers/Tenant/Roles';

export const metadata: Metadata = {
  title: 'Roles & Permissions | Gurukul',
  description: 'Manage roles and customize capabilities for your workspace.',
};

export default RolesContainer;
