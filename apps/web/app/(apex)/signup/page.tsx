import type { Metadata } from 'next';

import ApexSignup from '@/containers/Apex/Signup';

export const metadata: Metadata = {
  title: 'Create your workspace | Gurukul',
  description: 'Create a new Gurukul workspace and owner account.',
};

export default ApexSignup;
